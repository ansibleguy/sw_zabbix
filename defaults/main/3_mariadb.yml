---

ZABBIX_MARIADB_INSTANCE: "{{ {
    'dbs': {
      ZABBIX_CNF.database.name: {
        'encoding': 'utf8mb4',
        'collation': 'utf8mb4_bin'
      },
    },
    'users': {
      ZABBIX_CNF.database.user.server_proxy: {
        'priv': ZABBIX_CNF.database.name + '.*:ALL',
        'pwd': ZABBIX_CNF.database.pwd.server_proxy,
        'update_pwd': ZABBIX_CNF.database.update_password
      },
      ZABBIX_CNF.database.user.agent: {
        'priv': '*.*:PROCESS,SHOW DATABASES,BINLOG MONITOR,SHOW VIEW',
        'pwd': ZABBIX_CNF.database.pwd.agent,
        'update_pwd': ZABBIX_CNF.database.update_password
      },
    },
    'settings': ZABBIX_CNF.database.settings,
    'socket': '/run/mysqld/mysqld_zabbix.sock'
  } }}"  # DO NOT CHANGE!
